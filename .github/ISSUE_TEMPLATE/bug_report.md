---
name: Bug report
about: Report a problem with the MCP server
labels: bug
---

**Before you post: never paste credentials, refresh tokens, the contents of
your `credentials.yaml` (or equivalent creds file), or audit-log lines
(from `MS_ADS_AUDIT_PATH` JSONL) into this issue. Redact account IDs and
customer IDs in anything you do paste. Found a security issue instead of a
bug? Use the repo's Security tab → "Report a vulnerability" instead of an
issue.**

## What happened

<!-- What did you do, and what went wrong? -->

## Expected behavior

<!-- What did you expect to happen instead? -->

## Tool call (redacted)

<!-- Which MCP tool did you call, and with what arguments? Remove/replace any
account ID, customer ID, campaign name, or other account-specific value. -->

Tool name:

Arguments (redacted):

```
```

## Environment

- Python version:
- `bingads` package version:
